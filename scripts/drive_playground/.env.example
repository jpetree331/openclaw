# Copy this file to .env and fill in your values. Do not commit .env.
#   cp .env.example .env

# Your Google Drive folder ID (from the folder URL: drive.google.com/.../folders/<THIS_ID>).
DRIVE_PLAYGROUND_FOLDER_ID=

# Secret for OpenClaw to call this API. Use the same value in the Drive Playground plugin config (apiKey).
DRIVE_PLAYGROUND_API_KEY=

# Optional: path to OAuth client credentials if not in this directory as credentials.json
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json

# Optional: port (default 8765)
# PORT=8765
